fun main(){
    var names = arrayOf("john","Paul","Mark","Ringo","George")
    var counter = 0
    while (counter < names.size) {
        println(names[counter])
        counter++
    }
    //display new array
    var counter1 : Int = 0
    while (counter1 < names.size){
        println(names[counter1])

        counter1++
    }
}